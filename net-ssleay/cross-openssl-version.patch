--- a/Makefile.PL
+++ b/Makefile.PL
@@ -431,11 +431,27 @@
 
     {
         my $pipe = gensym();
-        open($pipe, qq{"$exec" version |})
-            or die "Could not execute $exec";
-        $output = <$pipe>;
-        chomp $output;
-        close $pipe;
+        if (open($pipe, qq{"$exec" version |})) {
+            $output = <$pipe>;
+            chomp $output if defined $output;
+            close $pipe;
+        }
+
+        # Fallback for cross-compilation: the openssl binary may be built for
+        # a different architecture and cannot execute on the build host.  Read
+        # the version from the header instead.
+        if (!defined $output || $output eq '') {
+            my $hdr = "$prefix/include/openssl/opensslv.h";
+            if (open(my $fh, '<', $hdr)) {
+                while (<$fh>) {
+                    if (/OPENSSL_VERSION_TEXT\s+"([^"]+)"/) {
+                        $output = $1;
+                        last;
+                    }
+                }
+                close $fh;
+            }
+        }
 
 	if ( ($major, $minor, $letter) = $output =~ /^OpenSSL\s+(\d+\.\d+)\.(\d+)([a-z]?)/ ) {
 	    print "*** Found OpenSSL-${major}.${minor}${letter} installed in $prefix\n";
