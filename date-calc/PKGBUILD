pkgname="carbonio-perl-date-calc"
pkgver="6.4" # renovate: datasource=cpan depName=Date::Calc
pkgrel="3"
pkgdesc="Gregorian calendar date calculations"
arch=('x86_64')
maintainer="Zextras <packages@zextras.com>"
url="https://github.com/zextras"
license=('Artistic-1.0-Perl OR GPL-1.0-or-later')
depends__apt=(
  "perl"
)
depends__yum=(
  "perl-core"
  "perl"
)
section="libs"
priority="important"
source=(
  "https://cpan.metacpan.org/authors/id/S/ST/STBEY/Date-Calc-${pkgver}.tar.gz"
)
sha256sums=('7ce137b2e797b7c0901f3adf1a05a19343356cd1f04676aa1c56a9f624f859ad')

build() {
  cd "${srcdir}/Date-Calc-${pkgver}"
  perl -I /opt/zextras/common/lib/perl5 Makefile.PL INSTALL_BASE=/opt/zextras/common \
    INSTALLSITEMAN1DIR=/opt/zextras/common/share/man/man1 INSTALLSITEMAN3DIR=/opt/zextras/common/share/man/man3 \
    LIB=/opt/zextras/common/lib/perl5
  make
}

package() {
  cd "${srcdir}/Date-Calc-${pkgver}"
  make install DESTDIR="${pkgdir}"
  find "${pkgdir}" -name '.packlist' -delete
  find "${pkgdir}" -name '*.pod' -delete
}
