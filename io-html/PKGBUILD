pkgname="carbonio-perl-io-html"
pkgver="1.004" # renovate: datasource=cpan depName=IO::HTML
pkgrel="3"
pkgdesc="Open an HTML file with automatic charset detection"
arch=('x86_64')
maintainer="Zextras <packages@zextras.com>"
url="https://github.com/zextras"
license=('Artistic-1.0-Perl OR GPL-1.0-or-later')
depends__apt=(
  "perl"
)
depends__yum=(
  "perl-core"
  "perl"
)
section="libs"
priority="important"
source=(
  "https://cpan.metacpan.org/authors/id/C/CJ/CJM/IO-HTML-${pkgver}.tar.gz"
)
sha256sums=('c87b2df59463bbf2c39596773dfb5c03bde0f7e1051af339f963f58c1cbd8bf5')

build() {
  cd "${srcdir}/IO-HTML-${pkgver}"
  perl -I /opt/zextras/common/lib/perl5 Makefile.PL INSTALL_BASE=/opt/zextras/common \
    INSTALLSITEMAN1DIR=/opt/zextras/common/share/man/man1 INSTALLSITEMAN3DIR=/opt/zextras/common/share/man/man3 \
    LIB=/opt/zextras/common/lib/perl5 LIBS="-L/opt/zextras/common/lib -lssl -lcrypto" INC="-I/opt/zextras/common/include"
  make
}

package() {
  cd "${srcdir}/IO-HTML-${pkgver}"
  make install DESTDIR="${pkgdir}"
  find "${pkgdir}" -name '.packlist' -delete
  find "${pkgdir}" -name '*.pod' -delete
}
